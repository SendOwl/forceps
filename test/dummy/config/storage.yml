test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

remote:
  service: Disk
  root: <%= Rails.root.join("tmp/storage_remote") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>
